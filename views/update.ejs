<!DOCTYPE html>
<html>
<head>
    <title>Update Todo</title>
</head>
<body>
    <h1>Update Todo</h1>

    <% if(todo) { %>
        <form action="/todo/update/<%= todo.id %>" method="post">
            <input type="text" name="task" value="<%= todo.task %>">
            <select name="status">
                <option value="pending" <% if(todo.status === 'pending') { %>selected<% } %>>Pending</option>
                <option value="completed" <% if(todo.status === 'completed') { %>selected<% } %>>Completed</option>
            </select>
            <button type="submit">Update</button>
        </form>
    <% } else { %>
        <p>Todo not found.</p>
    <% } %>

    <a href="/">Back to Todo List</a>
</body>
</html>
